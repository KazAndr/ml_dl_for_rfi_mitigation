# Configs

This directory is reserved for small reproducible run specifications.

No config is canonical yet. The first useful configs should appear after the
dataset contract and group-split correction are implemented.

Expected future configs:

- `dataset_b0531_group_split.yaml`
- `train_1dcnn_baseline.yaml`
- `train_statistical_baselines.yaml`
- `normalization_zscore_variants.yaml`
- `infer_filterbank_baseline.yaml`
- `infer_fits_offset_recheck.yaml`

Config files should contain parameters, not large data.
