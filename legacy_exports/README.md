# Legacy Exports

This directory contains Python exports from exploratory notebooks.

They are tracked for reference, but they are not yet a clean source package.
Stable reusable code should gradually move into `src/rfimt/`.
